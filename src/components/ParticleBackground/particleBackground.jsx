import Particles from 'react-particles';
import particleConfige from './particleConfige';

const ParticleBackground = () => {
  return <Particles params={particleConfige}></Particles>;
};

export default ParticleBackground;
